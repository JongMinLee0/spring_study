spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

# 다 없으면 spring boot가 메모리로 돌려버린다
# 기본적으로 create-drop으로 동작한다

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
